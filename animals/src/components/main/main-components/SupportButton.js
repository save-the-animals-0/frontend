import React, { Component } from 'react'

export class SupportButton extends Component {
    render() {
        return (
            <div>
                {/* Eventual onClick to send donation */}
                <button>Support</button>
            </div>
        )
    }
}

export default SupportButton
